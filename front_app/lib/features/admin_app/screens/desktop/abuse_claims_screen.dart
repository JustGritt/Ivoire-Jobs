import 'package:flutter/material.dart';

class AbuseClaimsScreen extends StatelessWidget {
  const AbuseClaimsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Abuse claims Screen'),
      ),
    );
  }
}